{
  "name": "fizzup-sales-manager",
  "version": "1.0.0",
  "description": "FizzUp Sales Manager",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node server.js",
    "install": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
